# to let a function return a value use the return statement 
def myfunction(x):
  return 5 * x

print(myfunction(3))
print(myfunction(5))
print(myfunction(9))
# the pass statement is used to avoid getting an error when a function is empty